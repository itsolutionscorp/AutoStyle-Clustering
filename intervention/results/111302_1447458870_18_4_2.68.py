def num_common_letters(goal_word, guess):
	return len(set(goal_word).intersection(list(guess)))

Positive Hints
...using a binary operation (<<, >>, &, ^, ~, |).

Negative Hints
...not using a call to list.

Approach Hints


Skeleton
