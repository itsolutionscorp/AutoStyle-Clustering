def num_common_letter(goal_word, guess):
	goal_word= "I give up"
	guess = "Yes"
	if goal_word==gues:
		return "What?"
	return (goal_word)


Positive Hints
...using a call to len....using a call to set....using a binary operation (<<, >>, &, ^, ~, |). Try to combine it with your call to set..

Negative Hints
...restructuring your function to not use a conditional.

Approach Hints
Think about what python construct/function you can use to get all the unique letters in each word. Then consider how you can compare these using a single operation. (HINT: use the suggestions above).

Skeleton
