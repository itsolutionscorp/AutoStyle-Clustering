public int multiply(int x,int y){
  int temp=0;
  if (x == 0 || y == 0) {
    return 0;
  }
  while (y != 0) {
    if ((y & 1) == 0) {
      x=x << 1;
      y=y >>> 1;
    }
 else {
      temp=add(temp,x);
      x=x << 1;
      y=y >>> 1;
    }
  }
  return temp;
}

