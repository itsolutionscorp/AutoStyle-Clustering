public int multiply(int x,int y){
  int counter=0;
  int result=0;
  while (y != 0) {
    if ((y & 1) == 1) {
      int xNew=x << counter;
      result=add(result,xNew);
    }
    y=y >> 1;
    counter+=1;
  }
  return result;
}

