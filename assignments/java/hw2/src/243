public int multiply(int x,int y){
  int sum=0;
  int shift=1;
  int xdig=x;
  for (int i=1; i < 32; i++) {
    int ydig=y & shift;
    if (ydig != 0) {
      sum=add(sum,xdig);
    }
    xdig=xdig << 1;
    shift=shift << 1;
  }
  return sum;
}

