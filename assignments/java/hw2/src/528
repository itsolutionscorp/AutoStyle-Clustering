public int multiply(int x,int y){
  if (y == 1) {
    return x;
  }
  int result=x << 1;
  int newy=y >> 1;
  if (y != add(newy,newy)) {
    return multiply(result,newy) + x;
  }
  return multiply(result,newy);
}

