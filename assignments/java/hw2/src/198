public int multiply(int x,int y){
  int and, returnVal;
  returnVal=0;
  while (x != 0) {
    and=x & 1;
    if (and != 0) {
      returnVal=add(returnVal,y);
    }
    x>>>=1;
    y<<=1;
  }
  return returnVal;
}

