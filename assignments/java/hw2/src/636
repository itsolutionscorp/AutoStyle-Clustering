public int multiply(int x,int y){
  int z=0;
  if (x < y) {
    int k=x;
    x=y;
    y=k;
  }
  while (y != 0) {
    if ((y & 1) != 0) {
      z=z + x;
    }
    x<<=1;
    y>>>=1;
  }
  return z;
}

