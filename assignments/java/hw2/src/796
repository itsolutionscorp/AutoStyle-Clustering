public int multiply(int x,int y){
  if (y < 0 && x > 0) {
    int temp=y;
    y=x;
    x=temp;
  }
  if (y < 0 && x < 0) {
    y=-y;
    x=-x;
  }
  int result=0;
  while (y != 0) {
    if ((y & 01) != 0) {
      result=add(result,x);
    }
    x<<=1;
    y>>=1;
  }
  return result;
}

