public int multiply(int x,int y){
  int result;
  if (y == 0 || x == 0) {
    return 0;
  }
  if (x == 1) {
    return y;
  }
 else {
    if ((x & 1) == 1) {
      return add(y,multiply(x >> 1,y << 1));
    }
 else {
      return multiply(x >> 1,y << 1);
    }
  }
}

