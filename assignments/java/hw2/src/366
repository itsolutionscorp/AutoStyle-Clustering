public int multiply(int x,int y){
  int new_x=x;
  int new_y=y;
  int answer=0;
  while (new_y != 0) {
    if ((new_y & 1) != 0) {
      answer=add(new_x,answer);
    }
    new_x=new_x << 1;
    new_y=new_y >>> 1;
  }
  return answer;
}

