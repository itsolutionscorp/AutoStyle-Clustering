public int multiply(int x,int y){
  int total=0;
  if (y < 0) {
    y=add(~y,1);
    if (x < 0) {
      x=add(~x,1);
    }
  }
  for (int i=0; i < y; i++) {
    total=add(total,x);
  }
  return total;
}

