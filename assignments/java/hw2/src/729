public int multiply(int x,int y){
  int sol=0;
  if (y == 0) {
    return 0;
  }
  if (x == 0) {
    return 0;
  }
 else {
    while (y != 0) {
      if ((y & 1) == 0) {
        sol=sol;
      }
 else {
        sol=add(sol,x);
      }
      x=x << 1;
      y=y >>> 1;
    }
    return sol;
  }
}

