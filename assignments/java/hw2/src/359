public int multiply(int x,int y){
  int result=0;
  int digits=32;
  for (int i=0; i < digits; i++) {
    if ((x & (1 << i)) == (1 << i)) {
      result=add(result,y << i);
    }
  }
  return result;
}

