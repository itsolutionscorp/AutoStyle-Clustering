public int multiply(int x,int y){
  int z=0;
  if ((x > y) || (x == y)) {
    while (y != 0) {
      if ((y & 1) != 0) {
        z=add(z,x);
      }
      x=x << 1;
      y=y >> 1;
    }
  }
 else {
    return multiply(y,x);
  }
  return z;
}

