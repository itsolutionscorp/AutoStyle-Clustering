public int multiply(int x,int y){
  int a=x, b=y, c=0;
  int inc=-1;
  if (b < 0) {
    inc=1;
    a=-a;
  }
  while (b != 0) {
    c=add(c,a);
    if (b > 0)     b=add(b,inc);
 else     b=add(b,inc);
  }
  return c;
}

