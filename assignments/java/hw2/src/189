public int multiply(int x,int y){
  if (x == 0 || y == 0) {
    return 0;
  }
  int b=getBit(y,0);
  if (b == 1) {
    return add(x,multiply(x << 1,y >>> 1));
  }
 else {
    return multiply(x << 1,y >> 1);
  }
}

