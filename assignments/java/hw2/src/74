public int multiply(int x,int y){
  int result=0;
  if (y < 0) {
    for (int i=0; i < add(~y,1); i++) {
      result=add(x,result);
    }
    result=add(~result,1);
  }
 else {
    for (int i=0; i < y; i++) {
      result=add(x,result);
    }
  }
  return result;
}

