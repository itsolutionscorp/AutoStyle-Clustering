public int multiply(int x,int y){
  Boolean check;
  int temp;
  while (y != 1) {
    temp=y >>> 1;
    check=temp == add(-temp,y);
    if (check == true) {
      x=x << 1;
      y=y >>> 1;
    }
 else {
      y=add(y,-1);
      return x + multiply(x,y);
    }
  }
  return x;
}

