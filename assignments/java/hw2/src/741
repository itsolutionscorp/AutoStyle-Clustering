public int multiply(int x,int y){
  int place=1;
  int product=0;
  while (y != 0) {
    if ((x & place) != 0) {
      product=add(product,y);
    }
    place=place << 1;
    y=y << 1;
  }
  return product;
}

