public int multiply(int x,int y){
  if (x == 0 || y == 0) {
    return 0;
  }
  if ((y & 1) == 1) {
    return add(x,multiply(x,y >>> 1) << 1);
  }
 else {
    return multiply(x,y >>> 1) << 1;
  }
}

