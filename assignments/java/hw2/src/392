public int multiply(int x,int y){
  if (y < 0) {
    x=add(0,-x);
    y=add(0,-y);
  }
  if (y == 0) {
    return y;
  }
  if ((y & 1) == 1) {
    return add(x,multiply(x,add(y,-1)));
  }
 else   return multiply(x << 1,y >> 1);
}

