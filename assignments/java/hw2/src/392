public int multiply(int x,int y){
  int answer=0;
  int shift=0;
  while (y != 0) {
    if ((y & 1) == 1) {
      answer=add(answer,(x << shift));
    }
    y=y >>> 1;
    shift=add(shift,1);
  }
  return answer;
}

