public int multiply(int x,int y){
  int result=0;
  while (y != 0) {
    int lastBit=y & 1;
    if (lastBit == 1) {
      result=add(result,x);
    }
    y=y >>> 1;
    x=x << 1;
  }
  return result;
}

