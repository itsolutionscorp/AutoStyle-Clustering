public int multiply(int x,int y){
  int total=0;
  if ((x & y) < 0) {
    x=~x;
    x=add(x,1);
    y=~y;
    y=add(y,1);
  }
  while (y != 0) {
    if ((y & 1) != 0) {
      total=add(total,x);
    }
    x=x << 1;
    y=y >> 1;
  }
  return total;
}

