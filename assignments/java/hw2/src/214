public int multiply(int x,int y){
  int i=1;
  int total=0;
  int numBits=32;
  while (i <= numBits) {
    if (((1 << i) & x) != 0) {
      total=add(total,(y << i));
    }
    i=i + 1;
  }
  return total;
}

