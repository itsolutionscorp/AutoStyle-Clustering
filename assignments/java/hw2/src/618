public int multiply(int x,int y){
  int result=0;
  int temp=0;
  while (y != 0) {
    for (int i=0; i < 32; i++) {
      temp=setBit(temp,i,(getBit(y,0) & getBit(x,i)));
    }
    result=add(result,temp);
    y=y >>> 1;
    x=x << 1;
  }
  return result;
}

