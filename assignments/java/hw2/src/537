public int multiply(int x,int y){
  int total=0;
  int curBit=1;
  int yesBit=0;
  while (x != 0) {
    yesBit=curBit & y;
    if (yesBit != 0) {
      total=add(total,x);
    }
    curBit=curBit << 1;
    x=x << 1;
  }
  return total;
}

