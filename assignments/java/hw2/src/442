public int multiply(int x,int y){
  int result=0;
  int counter=0;
  int mask, ybit;
  while (counter < 32) {
    mask=1 << counter;
    ybit=mask & y;
    if (ybit != 0) {
      result=add(result,multpow2(x,counter));
    }
    counter=counter + 1;
  }
  return result;
}

