public int multiply(int x,int y){
  int total=0;
  if ((y == 0) || (x == 0)) {
    return 0;
  }
 else   if (x == 1) {
    return y;
  }
  while (x != 0) {
    if ((x & 1) != 0) {
      total=add(total,y);
    }
    x=x >> 1;
    y=y << 1;
  }
  return total;
}

