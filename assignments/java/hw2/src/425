public int multiply(int x,int y){
  int mul=x;
  if (x == 0 || y == 0)   return 0;
  if (x == 1)   return y;
  if (y == 1)   return x;
  for (int i=1; i < y; i++) {
    mul=add(mul,x);
  }
  return mul;
}

