public int multiply(int x,int y){
  int total=0;
  int y_end=y & 1;
  int y_rest=y >>> 1;
  for (int i=0; i < 32; i++) {
    if (y_end == 1) {
      total=add(total,x);
    }
    x=x << 1;
    y_end=y_rest & 1;
    y_rest=y_rest >>> 1;
  }
  return total;
}

