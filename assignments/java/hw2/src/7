public int multiply(int x,int y){
  if (y < 0) {
    int opposite=flipSign(y);
    return flipSign(multiply(x,opposite));
  }
  int ans=0;
  while (y > 0) {
    ans=add(ans,x);
    y-=1;
  }
  return ans;
}

