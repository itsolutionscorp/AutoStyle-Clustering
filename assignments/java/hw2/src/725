public int multiply(int x,int y){
  int result=0;
  if (y == 0 || x == 0) {
    return 0;
  }
 else {
    while (y != 0) {
      if ((y & 1) != 0) {
        result=result + x;
      }
      x=x << 1;
      y=y >>> 1;
    }
  }
  return result;
}

