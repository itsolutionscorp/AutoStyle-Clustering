public int multiply(int x,int y){
  int finalResult=0;
  while (y != 0) {
    if ((y & 1) == 1) {
      finalResult=add(finalResult,x);
    }
    x=x << 1;
    y=y >>> 1;
  }
  return finalResult;
}

