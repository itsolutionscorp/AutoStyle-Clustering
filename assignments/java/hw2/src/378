public int multiply(int x,int y){
  if (x == 0 || y == 0) {
    return 0;
  }
  int digit;
  int total=0;
  int nthPower=0;
  while (y != 0) {
    digit=y & 1;
    if (digit == 1) {
      total=add(total,x << nthPower);
    }
    y=y >>> 1;
    nthPower+=1;
  }
  return total;
}

