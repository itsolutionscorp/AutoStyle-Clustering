public int multiply(int x,int y){
  int sum=0;
  int count=0;
  while (y != 0) {
    if ((y & 1) == 1) {
      sum=add(sum,x << count);
    }
    y=y >>> 1;
    count=add(count,1);
  }
  return sum;
}

