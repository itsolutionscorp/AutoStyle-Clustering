public int multiply(int x,int y){
  if (y == 1) {
    return x;
  }
  if (y == 0) {
    return 0;
  }
  if (add(y,1) == 0) {
    return add(~x,1);
  }
  int extra=0;
  if ((y & 1) != 0) {
    extra=x;
  }
  return add(extra,multiply(add(x,x),y >> 1));
}

