public int multiply(int x,int y){
  int z=0;
  int counter=0;
  if (y < 0) {
    y=add((~y),1);
    x=add((~x),1);
    while (counter < y) {
      z=add(z,x);
      counter+=1;
    }
  }
 else {
    while (counter < y) {
      z=add(z,x);
      counter+=1;
    }
  }
  return z;
}

