public int multiply(int x,int y){
  if (y == 0 || x == 0) {
    return 0;
  }
  if (y == 1) {
    return x;
  }
  if ((y & 1) == 1) {
    return add(multiply(x << 1,y >>> 1),x);
  }
  return multiply(x << 1,y >>> 1);
}

