public int multiply(int x,int y){
  if ((x == 0) || (y == 0)) {
    return 0;
  }
  if (x == 1) {
    return y;
  }
  if (y == 1) {
    return x;
  }
  int a=(y & 1);
  if (a == 1) {
    return (x + (multiply(x,y >>> 1) << 1));
  }
  return ((multiply(x,y >>> 1)) << 1);
}

