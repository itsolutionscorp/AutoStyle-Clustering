public int multiply(int x,int y){
  if (y == 0) {
    return 0;
  }
  int prod=0;
  int i=0;
  while (i < 32) {
    int remain=y & 1;
    if (remain == 1) {
      int newx=x << i;
      prod=add(prod,newx);
    }
    i=i + 1;
    y=y >> 1;
  }
  return prod;
}

