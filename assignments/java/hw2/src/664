public int multiply(int x,int y){
  if (x == 0 || y == 0) {
    return 0;
  }
  int ySign=getBit(y,31);
  if (ySign == 1) {
    x=negate(x);
    y=negate(y);
  }
  int product=0;
  for (int i=0; i < y; i++) {
    product=add(product,x);
  }
  return product;
}

