public int multiply(int x,int y){
  int result=0;
  if (y < 0) {
    y=add(~y,1);
    x=add(~x,1);
  }
  while (y != 0) {
    int val=y & 1;
    if (val != 0) {
      result=add(result,x);
    }
    x=x << 1;
    y=y >> 1;
  }
  return result;
}

