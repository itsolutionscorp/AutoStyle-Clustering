public int multiply(int x,int y){
  int xx=x, yy=y;
  int answer=0;
  while (xx != 0) {
    if ((xx & 1) != 0) {
      answer=add(answer,yy);
    }
    xx=xx >> 1;
    yy=yy << 1;
  }
  return answer;
}

