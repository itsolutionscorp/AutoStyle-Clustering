public int multiply(int x,int y){
  if (y == 0) {
    return 0;
  }
 else {
    if ((y & 1) == 1) {
      return add(x,multiply(x << 1,y >>> 1));
    }
    return add(0,multiply(x << 1,y >>> 1));
  }
}

