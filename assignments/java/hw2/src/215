public int multiply(int x,int y){
  int end=0;
  if (x < 0) {
    x=add(~x,1);
    y=add(~y,1);
  }
  while (x != 0) {
    if ((x & 1) == 1) {
      end=add(end,y);
    }
    y<<=1;
    x>>=1;
  }
  return end;
}

