public int multiply(int x,int y){
  int total=0;
  int mul=0;
  while (y != 0) {
    if ((y & 1) == 1) {
      total=add(total,x << mul);
    }
    mul=add(mul,1);
    y=y >>> 1;
  }
  return total;
}

