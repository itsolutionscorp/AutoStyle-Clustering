public int multiply(int x,int y){
  if ((x & 1) == 1) {
    return y + multiply(x >>> 1,y << 1);
  }
 else   if (x == 1) {
    return y;
  }
 else   if (x == 0 || y == 0) {
    return 0;
  }
 else {
    return multiply(x >>> 1,y << 1);
  }
}

