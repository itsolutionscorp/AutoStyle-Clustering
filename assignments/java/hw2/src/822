public int multiply(int x,int y){
  if (y == 0) {
    return 0;
  }
  int total=0;
  int counter=1;
  while (y != 0) {
    if ((y & 1) == 1) {
      total=add(total,x << (add(counter,-1)));
    }
    y=y >>> 1;
    counter=add(counter,1);
  }
  return total;
}

