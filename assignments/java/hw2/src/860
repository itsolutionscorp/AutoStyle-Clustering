public int multiply(int x,int y){
  Calculator calc=new Calculator();
  int product=0;
  int flag=0;
  if (y < 0) {
    y=~y + 1;
    flag=1;
  }
  for (int k=0; k < y; k++) {
    product=calc.add(product,x);
  }
  if (flag == 1) {
    product=~product + 1;
  }
  return product;
}

