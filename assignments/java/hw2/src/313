public int multiply(int x,int y){
  if (y < 0) {
    x=-x;
    y=-y;
  }
  if ((y & 1) == 1) {
    return add(multiply(x,add(y,-1)),x);
  }
 else   if (y == 0) {
    return 0;
  }
 else {
    return multiply((x << 1),(y >> 1));
  }
}

