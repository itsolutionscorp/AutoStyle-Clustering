public int multiply(int x,int y){
  int product=0;
  int bitNum=0;
  while (y != 0) {
    if ((y & 1) != 0) {
      product=add(product,x << bitNum);
    }
    y>>>=1;
    bitNum+=1;
  }
  return product;
}

