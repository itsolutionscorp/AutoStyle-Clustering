public int multiply(int x,int y){
  if (y == 0) {
    return 0;
  }
 else   if (y > 0) {
    y=add(2,y);
    y=add(y,add(~3,1));
    return add(x,multiply(x,y));
  }
 else {
    int a=~x;
    add(a,1);
    return add(-x,multiply(x,add(y,1)));
  }
}

