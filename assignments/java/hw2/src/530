public int multiply(int x,int y){
  if (y != 0) {
    if ((y & 1) != 0) {
      return add(x,multiply(x << 1,y >>> 1));
    }
 else {
      return multiply(x << 1,y >>> 1);
    }
  }
 else {
    return 0;
  }
}

