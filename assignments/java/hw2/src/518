public int multiply(int x,int y){
  int product=0;
  int digit;
  for (int i=0; y != 0; i=add(i,1)) {
    digit=y & 1;
    if (digit == 1) {
      product=add(product,x << i);
    }
    y=y >>> 1;
  }
  return product;
}

