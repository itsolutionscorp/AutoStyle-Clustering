public int multiply(int x,int y){
  int product=0;
  int counter=0;
  while (counter < 32) {
    if ((y & 1) == 1) {
      product=add(product,(x << counter));
    }
    y=y >> 1;
    counter++;
  }
  return product;
}

