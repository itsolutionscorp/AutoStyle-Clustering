public int multiply(int x,int y){
  if (y == 0) {
    return 0;
  }
  if (y > 0) {
    return add(x,multiply(x,add(y,-1)));
  }
 else   if (y < 0) {
    return add(~(multiply(x,-y)),1);
  }
  return 0;
}

