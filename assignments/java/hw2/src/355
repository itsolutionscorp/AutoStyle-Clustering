public int multiply(int x,int y){
  int product=0;
  int n=0;
  while (n < sizeOfInt) {
    int last_bitY=y & 0x1;
    if (last_bitY == 1) {
      product=add(product,x);
    }
    x=x << 1;
    y=y >> 1;
    n+=1;
  }
  return product;
}

