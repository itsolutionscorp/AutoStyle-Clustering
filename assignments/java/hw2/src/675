public int multiply(int x,int y){
  int sum=0;
  int iter=0;
  while (y != 0) {
    int bit=getBit(y);
    if (bit == 1) {
      sum=add(sum,x << iter);
    }
    y=y >>> 1;
    iter++;
  }
  return sum;
}

