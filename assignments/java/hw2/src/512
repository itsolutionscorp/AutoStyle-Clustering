public int multiply(int x,int y){
  if (x == 0 || y == 0) {
    return 0;
  }
  int a=x;
  int b=y;
  int ans=0;
  while (b != 0) {
    if ((b & 1) != 0) {
      ans=add(ans,a);
    }
    a=a << 1;
    b=b >>> 1;
  }
  return ans;
}

