public int multiply(int x,int y){
  if (x == 0 || y == 0)   return 0;
  if (y < 0)   return ~multiply(x,(~y) + 1) + 1;
 else {
    int ans=0, m=0;
    while (y > 0) {
      if ((y & 1) == 1) {
        ans=add(ans,x << m);
      }
      y=y >>> 1;
      m=add(m,1);
    }
    return ans;
  }
}

