public int multiply(int x,int y){
  if (x == 0) {
    return 0;
  }
  int answer=0;
  for (int i=0; i < Math.abs(x); i++) {
    answer=add(answer,y);
  }
  if (x < 0) {
    answer=~answer;
    answer=add(answer,1);
  }
  return answer;
}

