public int multiply(int x,int y){
  int soln=0, xt=x, yt=y;
  if (yt < 0) {
    yt=add(~yt,1);
  }
  while ((yt != 0 && yt != -1)) {
    if ((yt & 1) != 0) {
      soln=add(soln,xt);
    }
    xt=xt << 1;
    yt=yt >> 1;
  }
  if (y < 0) {
    return add(~soln,1);
  }
  return soln;
}

