public int multiply(int x,int y){
  int j=1;
  int val=0;
  int i=0;
  for (i=0; i < 32; i++)   if ((y & j) == 1) {
    val=add(val,x);
    y=y >> (1);
    x=x << (1);
  }
 else {
    y=y >> (1);
    x=x << (1);
  }
  return val;
}

