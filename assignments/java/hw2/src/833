public int multiply(int x,int y){
  boolean sign=true;
  if (y < 0) {
    sign=false;
    y=add(~y,1);
  }
  int sum=0;
  while (y > 0) {
    if ((y & 1) == 1) {
      sum=add(sum,x);
    }
    y=y >> 1;
    x=x << 1;
  }
  if (sign == true) {
    return sum;
  }
  return add(~sum,1);
}

