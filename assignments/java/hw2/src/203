public int multiply(int x,int y){
  int sum=0;
  if (x < 0 && y < 0) {
    x=add(~x,1);
    y=add(~y,1);
  }
  if (y < x) {
    int temp=y;
    y=x;
    x=temp;
  }
  for (int i=0; i < y; i++) {
    sum=add(sum,x);
  }
  return sum;
}

