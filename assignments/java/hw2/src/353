public int multiply(int x,int y){
  int ans=0;
  if ((x < 0) && (y < 0)) {
    x=add(~x,1);
    y=add(~y,1);
  }
 else   if (y < 0) {
    x=add(~x,1);
    y=add(~y,1);
  }
  while (y > 0) {
    if ((y & 1) != 0) {
      ans=add(ans,x);
    }
    x=x << 1;
    y=y >> 1;
  }
  return ans;
}

