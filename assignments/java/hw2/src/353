public int multiply(int x,int y){
  int result=0, i=0, terms;
  while (y != 0) {
    if ((y & 1) == 1)     terms=x << i;
 else     terms=0;
    y=y >>> 1;
    i=this.add(i,1);
    result=this.add(result,terms);
  }
  return result;
}

