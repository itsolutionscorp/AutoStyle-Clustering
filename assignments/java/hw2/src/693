public int multiply(int x,int y){
  if (y == 0) {
    return 0;
  }
 else   if (y > 0) {
    return (add(x,multiply(x,add(y,add(~1,1)))));
  }
 else {
    return add(~(multiply(x,add(~y,1))),1);
  }
}

