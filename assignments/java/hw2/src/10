public int multiply(int x,int y){
  if (x < 0) {
    x=~x + 1;
    y=~y + 1;
  }
  int position=1;
  int total=0;
  while (position <= x) {
    if ((x & position) == position)     total=add(total,y);
    y=y << 1;
    position=position << 1;
  }
  return total;
}

