public int multiply(int x,int y){
  if (x == 0) {
    return 0;
  }
 else {
    int sum=0;
    int ct=0;
    while (y != 0) {
      if ((y & 1) == 1) {
        sum=this.add(sum,x << ct);
      }
      y=y >>> 1;
      ct++;
    }
    return sum;
  }
}

