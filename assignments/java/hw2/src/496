public int multiply(int x,int y){
  if (x < y) {
    int placeholder=x;
    x=y;
    y=placeholder;
  }
  int result=0;
  while (y != 0) {
    if ((y & 01) != 0) {
      result=add(result,x);
    }
    x=x << 1;
    y=y >>> 1;
  }
  return result;
}

