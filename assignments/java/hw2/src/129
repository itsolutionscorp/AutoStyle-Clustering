public int multiply(int x,int y){
  if (x == 0 || y == 0)   return 0;
  if (y == 1)   return add(x,0);
 else   if (y % 2 == 0)   return multiply(add(x,x),y >> 1);
 else   return add(x,multiply(add(x,x),y >> 1));
}

