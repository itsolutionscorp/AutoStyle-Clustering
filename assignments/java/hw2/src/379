public int multiply(int x,int y){
  int ans=0;
  if (y > x) {
    int temp=x;
    x=y;
    y=temp;
  }
  while (y != 0) {
    if ((y & 1) != 0) {
      ans=add(ans,x);
    }
    x=x << 1;
    y=y >>> 1;
  }
  return ans;
}

