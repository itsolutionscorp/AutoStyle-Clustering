public int multiply(int x,int y){
  int result=0, count=0;
  while (count < 32) {
    if ((y & 1) != 0) {
      result=add(result,x);
    }
    x=x << 1;
    y=y >>> 1;
    count++;
  }
  return result;
}

