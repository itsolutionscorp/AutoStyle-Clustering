public int multiply(int x,int y){
  int product;
  product=0;
  while (y != 0) {
    if ((y & 1) != 0) {
      product=product + x;
    }
    y=y >>> 1;
    x=x << 1;
  }
  tempeq=(x + " * " + y);
  result=product;
  saveEquation(tempeq,result);
  return product;
}

