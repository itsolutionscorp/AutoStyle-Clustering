public int multiply(int x,int y){
  int sum=0;
  if (x == 0 || y == 0) {
    return 0;
  }
 else {
    while (y != 0) {
      if ((y & 01) != 0) {
        sum=add(sum,x);
      }
      y=y >> 1;
      x=x << 1;
    }
    return sum;
  }
}

