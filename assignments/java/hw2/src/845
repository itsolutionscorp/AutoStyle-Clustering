public int multiply(int x,int y){
  int productSum=0;
  while (y != 0) {
    if ((y % 2) != 0) {
      productSum=this.add(productSum,x);
    }
    y=y >>> 1;
    x=x << 1;
  }
  return productSum;
}

