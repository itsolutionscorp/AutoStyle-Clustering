public int multiply(int x,int y){
  if (x > y) {
    return multiply(y,x);
  }
 else {
    int multiplicand=0;
    while (y != 0 && y != -1) {
      if ((y & 1) == 1) {
        multiplicand=this.add(multiplicand,x);
      }
      x=x << 1;
      y=y >> 1;
    }
    return multiplicand;
  }
}

