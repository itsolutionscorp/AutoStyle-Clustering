public int multiply(int x,int y){
  if (y == 0 || x == 0) {
    return 0;
  }
 else   if (y == 1) {
    return x;
  }
  int extra=0;
  if ((y & 1) != 0) {
    extra=x;
  }
  return add(extra,multiply(x << 1,y >> 1));
}

