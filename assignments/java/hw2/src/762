public int multiply(int x,int y){
  int index=0;
  int product=0;
  while (index < 32) {
    if (getBit(y,index) == 1) {
      product=add(product,(x << index));
    }
    index=index + 1;
  }
  return product;
}

