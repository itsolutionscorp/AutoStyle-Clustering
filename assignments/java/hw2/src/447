public int multiply(int x,int y){
  if (y == 0) {
    return 0;
  }
  int t=~(-1 >>> 1);
  int b=y & t;
  while (b == 0) {
    t=t >>> 1;
    b=y & t;
  }
  int c=x;
  int d=b;
  while (d != 1) {
    c=c << 1;
    d=d >>> 1;
  }
  int e=(y ^ b);
  return add(c,multiply(x,e));
}

