public int multiply(int x,int y){
  int result=0;
  if (x < 0 && y < 0) {
    x=add(~x,1);
    y=add(~y,1);
  }
 else   if (x > 0 && y < 0) {
    int temp=x;
    x=y;
    y=temp;
  }
  for (int i=0; i < y; i++) {
    result=add(result,x);
  }
  return result;
}

