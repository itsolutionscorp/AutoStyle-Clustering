public int multiply(int x,int y){
  if (y < 0 & x < 0) {
    x=add(~x,1);
    y=add(~y,1);
  }
 else   if (y < 0) {
    int tmp=x;
    x=y;
    y=tmp;
  }
  int product=0;
  int k=0;
  while (k < y) {
    product=add(product,x);
    k+=1;
  }
  return product;
}

