public int multiply(int x,int y){
  int total=0;
  int temp=0;
  if (y < 0 && x > 0) {
    temp=y;
    y=x;
    x=temp;
  }
 else   if (y < 0 && x < 0) {
    x=add((~x),1);
    y=add((~y),1);
  }
  for (int i=0; i < y; i++) {
    total=add(total,x);
  }
  return total;
}

