public int multiply(int x,int y){
  int current=0;
  while (y != 0) {
    if (x == 0) {
      return current;
    }
    if ((y & 1) != 0) {
      current=add(current,x);
    }
    x=x << 1;
    y=y >> 1;
  }
  return current;
}

