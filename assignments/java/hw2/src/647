public int multiply(int x,int y){
  int result=0;
  if (x < 0) {
    x=add(~x,1);
    y=add(~y,1);
  }
  while (y != 0) {
    if ((y & 1) != 0) {
      result=add(result,x);
    }
    x<<=1;
    y>>>=1;
  }
  return result;
}

