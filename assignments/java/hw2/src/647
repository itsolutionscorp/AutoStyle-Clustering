public int multiply(int x,int y){
  int z=1;
  int sum=0;
  if (x < 0) {
    x=add(~x,1);
    y=add(~y,1);
  }
  while ((x >= z) && (y != 0)) {
    if ((x & z) != 0) {
      sum=add(sum,y);
    }
    y=y << 1;
    z=z << 1;
  }
  return sum;
}

