public int multiply(int x,int y){
  int product=0;
  for (int i=0; i < 32; i++) {
    int tempy=(y >>> i) & 1;
    int tempx=0;
    if (tempy == 1) {
      tempx=(x << i);
    }
    product=this.add(product,tempx);
  }
  return product;
}

