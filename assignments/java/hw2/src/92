public int multiply(int x,int y){
  int total=0;
  int power=0;
  for (int i=0; i < 32; i++) {
    if ((y & 1) == 1) {
      total=add(total,x << power);
    }
    y=y >>> 1;
    power=add(power,1);
  }
  return total;
}

