public int multiply(int x,int y){
  int result=0;
  if (y < 0) {
    x=add(~x,1);
    y=add(~y,1);
  }
  while (y != 0) {
    if ((y & 1) == 1) {
      result=add(result,x);
    }
    y=y >> 1;
    x=x << 1;
  }
  return result;
}

