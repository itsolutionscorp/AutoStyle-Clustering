public int multiply(int x,int y){
  int collector=0;
  while (x != 0) {
    int lastBit=1 & y;
    if (lastBit == 1) {
      collector=add(collector,x);
    }
    y=y >> 1;
    x=x << 1;
  }
  return collector;
}

