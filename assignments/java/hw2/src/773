public int multiply(int x,int y){
  int store=0;
  while (y != 0) {
    if (getBit(y,0) == 1) {
      store=add(store,x);
      y=y >> 1;
      x=x << 1;
    }
    y=y >> 1;
    x=x << 1;
  }
  return store;
}

