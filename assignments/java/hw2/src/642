public int multiply(int x,int y){
  if (y == 0) {
    return 0;
  }
  int product=0;
  while (y != 0) {
    if ((y & 1) == 1) {
      product=add(product,x);
    }
    x=x << 1;
    y=y >>> 1;
  }
  return product;
}

