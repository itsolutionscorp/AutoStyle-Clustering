public int multiply(int x,int y){
  int place_holder=0;
  while (x != 0) {
    if ((x & 1) == 1) {
      place_holder=add(place_holder,y);
    }
    x=x >>> 1;
    y=y << 1;
  }
  return place_holder;
}

