public int multiply(int x,int y){
  int total=0;
  for (int i=0; y != 0; i++) {
    int shouldShift=(y & 1);
    if (shouldShift == 1) {
      total=add(total,(x << i));
    }
    y=y >>> 1;
  }
  return total;
}

