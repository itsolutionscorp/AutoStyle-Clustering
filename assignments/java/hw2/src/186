public int multiply(int x,int y){
  if (y == 0) {
    return 0;
  }
  if (y == 1) {
    return x;
  }
  if (y == -1) {
    return add((~x),1);
  }
  if ((y ^ 1) != add(y,1)) {
    return add(x,multiply(x,(y ^ 1)));
  }
  x=x << 1;
  y=y >> 1;
  return multiply(x,y);
}

