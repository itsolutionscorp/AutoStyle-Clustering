public int multiply(int x,int y){
  if (y < 0) {
    y=~y;
    y=add(y,1);
    x=~x;
    x=add(x,1);
    return multiply(x,y);
  }
 else {
    int result=0;
    while (y != 0 && x != 0) {
      result=add(result,x);
      y=add(y,-1);
    }
    return result;
  }
}

