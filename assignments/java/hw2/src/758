public int multiply(int x,int y){
  int xtemp=x;
  for (int i=1; i < Math.abs(y); i++) {
    x=add(xtemp,x);
  }
  if (y == 0) {
    return 0;
  }
  if ((y >>> 31) == 1) {
    x=~x;
    x=add(x,1);
  }
  return x;
}

