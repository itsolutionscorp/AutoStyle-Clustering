public int multiply(int x,int y){
  int result=0;
  if (x < 0 && y < 0) {
    x=add(~x,1);
    y=add(~y,1);
  }
  if (x < 0) {
    int temp=x;
    x=y;
    y=temp;
  }
  while (x > 0) {
    if ((x & 1) != 0) {
      result=add(y,result);
    }
    x=x >> 1;
    y=y << 1;
  }
  return result;
}

