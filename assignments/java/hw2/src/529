public int multiply(int x,int y){
  if ((x < 0) && (y < 0)) {
    x=-x;
    y=-y;
  }
  int product=0;
  while (y != 0) {
    if ((y & 01) != 0) {
      product=add(product,x);
    }
    x=x << 1;
    y=y >> 1;
  }
  return product;
}

