public int multiply(int x,int y){
  int total=0;
  boolean isNegative=false;
  if (y < 0) {
    y=~y + 1;
    isNegative=true;
  }
  for (int i=0; i < y; i++) {
    total=add(x,total);
  }
  if (isNegative == true)   return ~total + 1;
  return total;
}

