public int multiply(int x,int y){
  int sum=0;
  int a=0;
  for (int i=0; i < 32; i++) {
    a=((1 << 0) & y);
    if (a == 1) {
      sum=add(sum,(x << i));
    }
    y=y >>> 1;
  }
  return sum;
}

