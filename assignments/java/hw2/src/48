public int multiply(int x,int y){
  if (y < 0) {
    int temp=x;
    x=y;
    y=temp;
  }
  if (y < 0 && x < 0) {
    x=-x;
    y=-y;
  }
  int sum=0;
  while (y != 0) {
    if ((y & 1) != 0)     sum=add(sum,x);
    x=x << 1;
    y=y >> 1;
  }
  return sum;
}

