public int multiply(int x,int y){
  if (y == 0) {
    return 0;
  }
  if (y == 1) {
    return x;
  }
  if (y < 0) {
    return -multiply(x,add(1,~y));
  }
  if ((1 & y) != 0) {
    return add(x,multiply(x,1 ^ y));
  }
 else {
    return multiply(x << 1,y >> 1);
  }
}

