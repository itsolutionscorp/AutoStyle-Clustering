public int multiply(int x,int y){
  if ((y < 0) || (x < 0)) {
    y=negative(y);
    x=negative(x);
  }
  int val=0;
  while (y > 0) {
    if ((y & 1) != 0) {
      val=add(val,x);
    }
    y=y >> 1;
    x=x << 1;
  }
  return val;
}

