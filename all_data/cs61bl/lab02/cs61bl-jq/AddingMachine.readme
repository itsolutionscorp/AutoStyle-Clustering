1. We used the single loop approach.
2. One loop would constantly count the total, adding inputs to the total variable. The other loop would keep track of subtotals and display them when a 0 is entered.
3. We prefer the one loop version because it makes the code more simple.
